plugins {
    id 'java-library'
}

group 'com.github.badaccuracyid.legendarycomputingmachine'
version '0.1'

java.sourceCompatibility = JavaVersion.VERSION_11

tasks.withType(JavaCompile).configureEach {
    options.encoding = 'UTF-8'
}